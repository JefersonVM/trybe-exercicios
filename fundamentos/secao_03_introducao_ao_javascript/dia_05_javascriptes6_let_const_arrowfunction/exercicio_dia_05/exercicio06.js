/* function hello(nome) {
    return `Olá, ${nome}!`
  }
  let nome = 'Ivan';
  console.log(hello(nome)); */

  let hello = (nome) => `Olá, ${nome}!`
  let nome = `Ivan`;
  console.log(hello(nome));
  